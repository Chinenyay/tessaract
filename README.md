A model-agnostic API for building AI agents. Tessaract exposes a canonical model of the LLM ecosystem - requests, responses, content, streaming events, tools, usage, error, reasoning and provider-specific capabilities - and then maps each provider onto that model.

BUILD IN PROGRESS
