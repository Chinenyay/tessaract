An API layer for building applications on top of multiple LLM provider APIs. Tessaract exposes a canonical model of the LLM ecosystem - requests, responses, content, streaming events, tools, usage, error, reasoning and provider-specific capabilities - and then maps each provider onto that model.

BUILD IN PROGRESS